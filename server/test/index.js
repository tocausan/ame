let chai = require('chai');

describe('data-access', () => {
  require('./data-access');
});

describe('models', () => {
  require('./models');
});


describe('services', () => {
  require('./services');
});
